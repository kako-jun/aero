# Aero — 開発ガイド

## コンセプト

`aero` は、耳の聞こえない人・聞こえにくい人が、**視界外で起きた呼びかけや周囲の異変に気づくための小さな常駐アプリ**。

字幕や会話理解を主目的にしない。まずは **「何か起きた」ことに気づけること** に絞る。

## 技術スタック

- **フロントエンド**: Vite + TypeScript（`frontend/`）
- **desktop shell**: Tauri v2（`src-tauri/`）
- **第一対応 OS**: Windows
- **CI**: 後で追加。まずは desktop MVP の成立を優先

## ローカル起動

```bash
cd frontend && npm install
npm run dev
npm run tauri:dev
```

## UI 方針

- 右下に小さいインジケータを置く
- 平常時は緑の小さな丸
- 周囲の音が急に強くなったら黄 → 赤へ変化
- 切迫時はトゲトゲ吹き出し形状にする
- 数値や波形を前面に出しすぎない
- 作業中に邪魔にならないことを優先する

## 実装方針

- まずは **方向表示をやらない**
- まずは **平常との差分で異変を検知** する
- 音は保存しない
- クラウド送信しない
- 将来的に `voice-like / object-like` 程度の簡易分類を検討する

## リポジトリ方針

- `README.md` / `README.ja.md` は **エンドユーザー向け**
- 実装・設計・判断は `docs/` とこのファイルに書く
- `esuna` / `libra` の外形には寄せるが、`aero` は Web アプリではなく desktop アプリ
- `backend/` は置かず、`frontend/ + src-tauri/` 構成にする

## 禁止事項

- Co-Authored-By をコミットメッセージに付けない
- 方向検知や個人識別を最初から抱え込まない
